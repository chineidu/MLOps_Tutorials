model:
  name: resnet18  # Specify model name as resnet18

# Training configuration
train:
  # Adjust these values as needed for your training
  batch_size: 32
  epochs: 100
  optimizer:
    name: adam  # Common optimizer choice, adjust if needed
    learning_rate: 0.001  # Starting learning rate

# Data path (replace with your actual data paths)
data:
  train_dir: /path/to/your/training/data
  val_dir: /path/to/your/validation/data
